apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ./ingress.yml

labels:
  - pairs:
      app.kubernetes.io/name: shinsei-manager-front
    includeSelectors: true

images:
  - name: placeholder
    newName: ${CONTAINER_IMAGE}
    newTag: ${IMAGE_TAG}
