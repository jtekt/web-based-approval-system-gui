apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: shinsei-manager-front
spec:
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
  hostnames:
    - approvals.jtektrnd.net
  rules:
    - backendRefs:
        - name: shinsei-manager-front
          port: 80
