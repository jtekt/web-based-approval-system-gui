resources:
  - ../../base
  - ./ingress.yml

nameSuffix: -staging

# Inject and update labels across resources, selectors, and service references
labels:
  - pairs:
      app.kubernetes.io/name: shinsei-manager-front-staging
    includeSelectors: true

patches:
  - path: env-patch.yml
